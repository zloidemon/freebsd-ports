
$FreeBSD$

--- configure.orig
+++ configure
@@ -13091,8 +13091,8 @@
 fi
 
  if test "$LIBCRYPT_LIBS" != "" ; then
-  HAVE_LIBCRYPT_TRUE=
-  HAVE_LIBCRYPT_FALSE='#'
+  HAVE_LIBCRYPT_TRUE='#'
+  HAVE_LIBCRYPT_FALSE=
 else
   HAVE_LIBCRYPT_TRUE='#'
   HAVE_LIBCRYPT_FALSE=
